export enum TargetType {
  LED = 'led',
  FAN = 'fan',
  AUTO_FAN = 'autoFan',
}

export enum LedFanAction {
  ON = 'on',
  OFF = 'off',
}

export enum AutoFanAction {
  ENABLE = 'enable',
  DISABLE = 'disable',
}

export enum ControlSource {
  USER = 'user',
  AUTO = 'auto',
}
